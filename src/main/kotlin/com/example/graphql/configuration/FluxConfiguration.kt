package com.example.graphql.configuration

/*@Configuration
@EnableWebFlux
class FluxConfiguration : WebFluxConfigurer {
	override fun addCorsMappings(corsRegistry: CorsRegistry) {
		corsRegistry.addMapping("/**")
			.allowedOrigins("*")
			.maxAge(3600)
	}
}

*/
 */

